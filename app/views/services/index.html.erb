<h1>Services</h1>
<table class="table table-hover">
  <thead>
  <tr>
    <th class="col"><%= t ".description" %></th>
    <th class="col"><%= t ".price" %></th>
    <th class="col"><%= t ".actions" %></th>
  </tr>
  </thead>

  <tbody>
  <% @active_services.each do |active_price| %>
    <tr>
      <td><%= active_price.description %></td>
      <td>£<%= active_price.price %></td>
      <td>magic</td>
    </tr>
  <% end %>
  </tbody>
</table>
