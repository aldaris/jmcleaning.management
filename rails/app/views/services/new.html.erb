<div>
  <h1><%= t ".heading" %></h1>
  <div class="card">
    <h5 class="card-header"><i class="fas fa-file-invoice-dollar"></i> <%= t ".details" %></h5>
    <div class="card-body">
      <%= simple_form_for @service do |form| %>
        <%= form.input :description %>
        <%= form.input :price, input_html: {min: 1, step: 0.5}, wrapper: :input_group, prepend: "£" %>
        <%= form.input :is_active %>
        <%= form.submit class: "btn btn-primary btn-block" %>
      <% end %>
    </div>
  </div>
</div>
